// This is the single source of truth for your server's address.
// Replace with your primary PC's Local IP Address.
export const SERVER_IP = '192.168.0.127';

export const SERVER_URL = `http://${SERVER_IP}:8080`;